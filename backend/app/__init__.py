"""NyayaChakshu enforcement backend.

A credible API + tool layer behind the NyayaChakshu command-centre console.
Implements the report's real subsystems: the SHA-256 evidence hash chain,
the ALPR (Vahan/HSRP) plate normaliser, the MV Act penalty engine, and the
TrackedObject / ViolationRecord / EvidencePackage schemas.
"""

__version__ = "1.0.0"
