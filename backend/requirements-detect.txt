# Local real-detection stack (Python 3.11–3.13; torch has no 3.14 wheels yet).
# Install into a dedicated venv:  python3.11 -m venv .venv311
fastapi>=0.110
pydantic>=2.6
uvicorn[standard]>=0.27
python-multipart>=0.0.9
ultralytics>=8.3.0
pillow>=10.0
